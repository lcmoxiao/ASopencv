<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wocaowocao">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_me"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_me"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">


        <activity android:name=".base.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recogImg.recogImgActivity" />
        <activity android:name=".fileManager.depositoryActivity" />
        <activity android:name=".elf.CoreActivity" />

        <service android:name=".elf.recordservice.rFloatService" />
        <service android:name=".elf.recordservice.RecordService" />
        <service android:name=".elf.simulateservice.simulateFloatService" />
        <service android:name=".elf.shotService" />
    </application>

</manifest>