<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wocaowocao">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <receiver android:name=".receiver.shotReceiver" />
        <receiver android:name=".receiver.finishedReceiver" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recogImg.recogImgActivity" />
        <activity android:name=".fileManager.fmanagerActivity" />
        <activity android:name=".shotActivity" />

        <service android:name=".recordservice.rFloatService" />
        <service android:name=".recordservice.RecordService" />
        <service android:name=".simulateservice.sFloatService" />
        <service android:name=".simulateservice.SimulateService" />
        <service android:name=".shotService" />
    </application>

</manifest>